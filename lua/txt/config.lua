return {
    glider = "ZK-GZZ",
    retrieve_phone = "+64987654321",
    opsnormal_phone = "+64123456789",
    unit = "metric",                    --or "feet" (default)
    format = "g-maps",                  --or "simple" (default)
    gsm_modem_config = {
        ip = "localhost",
        port = "8787",                  --http://xxx.xxx.xxx.xxx:[port] in your "GSM Modem" Android app
        username = "usr",               --you'll set this up in your "GSM Modem" Android app (USERS)
        password = "zzz",               --same
    }
}
